# tidyverse, Hmisc

# Week 1-4
library(psych) # describe()
library(dplyr) # na_if() and recode() - lec3

library(gmodels) # for `CrossTable()` -- pie chart - lec7
library(ggplot2)
library(forcats) # fct_infreq(x)


# Final exam

library(gmodels) # for `CrossTable()`
library(sjstats) # for cramer'V test - `crosstable_statistics()`
library(sjPlot) # for `sjPlot::sjt.xtab()`

library(GDAtools) # for `pem()`

library(glm2)
library(LogisticDx)  # for `gof()`
library(jtools)      # for `export_summs()`

library(car)     # for `vif()` ==> Collinearity checking

# Multinomial Logistic Regression
library(nnet)
library(stargazer)

library(generalhoslem)  # for `logitgof()`
library(DescTools)      # for `PseudoR2()`
library(summarytools)
